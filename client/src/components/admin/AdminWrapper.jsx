import React from 'react';

const AdminWrapper = ({ children }) => {
    return (
        <div className="w-full h-full font-sans text-[#1e2e4a]">
            {children}
        </div>
    );
};

export default AdminWrapper;
